#include <stdio.h>
int main()
{
    int a[]={5,4,7,2,3},i,l,s;
    l=a[0];
    s=a[0];

    for(i=0;i<5;i++)
    {
        if (l<a[i]);
    }
else if (s>a[i])
    {
        s=a[i];
    }
}
printf("Large= %d\nSmall= %d,l,s");
return 0;

